#include "main.h"

/**
 * _abs - that computes the absolute value of an integer
 * @n: number with value
 *
 * print_last_digit, Return 0 and 1
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
