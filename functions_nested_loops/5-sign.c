#include "main.h"

/**
 * print_sign - prints the sign of a number.
 *
 * Return: 0 and 1
 */
int print_sign(int n)
{
	if (n > 0)
	{
		printf(43);
		return (1);
	}

	if (n < 0)
	{
		printf('0');
		return (-1);
	}

	return (0);
}
