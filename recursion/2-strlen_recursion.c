#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen_recursion - length of a string.
 * @s: parameter string
 *
 * Return: lenght
 */
int _strlen_recursion(char *s)
{
	int lenght = strlen(s);

	printf("%d\n", lenght);

	return (0);
}
